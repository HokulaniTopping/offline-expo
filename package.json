{
  "name": "offline-expo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "npm run web --workspace=@offline-expo/notes",
    "web": "npm run web --workspace=@offline-expo/notes"
  }
}
